ENG:
To run the application -> start "startApp.sh" script or use command "sudo docker-compose up" from /Travail/ directory.
The app will start on localhost:8000

FR:
Pour lancer l'application -> lancer le script "startApp.sh" ( sur Linux ) ou utilisez la commande "sudo docker-compose up" depuis le dossier Travail/
L'application se lancera à l'adresse localhost:8000
